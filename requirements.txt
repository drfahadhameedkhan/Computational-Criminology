# Core dependencies for the Python track of Computational Criminology.
# Versions are pinned to the lower bounds the examples were tested against;
# newer releases should also work. Install with:  pip install -r requirements.txt

numpy>=1.26
pandas>=2.0
matplotlib>=3.7
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
networkx>=3.0
requests>=2.31

# The spatial chapter (Chapter 9) additionally needs the geospatial stack.
# These can be heavy to install locally; on Google Colab they install cleanly.
# Uncomment if you want them in the base environment:
# geopandas>=0.14
# libpysal>=4.9
# esda>=2.5
# shapely>=2.0
